{
  // Kept as a separate entry point because `pnpm lint` names it, but it is deliberately just
  // the editor project next door — same sources, same path mappings, same settings. Anything
  // added here and not there would be an error the editor cannot see.
  "extends": "./tsconfig.json"
}

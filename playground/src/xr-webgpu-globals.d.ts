// The playground type-checks Babylon Lite through its source alias, so provide the
// draft runtime global that is normally included by the library's own tsconfig.
declare const XRGPUBinding: import("babylon-lite").XrGpuBindingConstructor;
